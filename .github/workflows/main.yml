 name: publish

on: [push]

jobs:
  publish-hello-world-image:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Build the hello-world Docker image
      run: |
        echo $CR_PAT | docker login ghcr.io -u Rahul6bravo --password-stdin
      env:
        CR_PAT: ${{ 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 }}
